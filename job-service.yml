server:
  port: 8081
spring:
  application:
    name: job-service
  datasource:
    url: jdbc:postgresql://localhost:5432/talenthub_job_db
    username: admin
    password: Nguyenanphu2005@
  jpa:
    hibernate:
      ddl-auto: validate    # dev only, prod đổi qua Flyway
    properties:
      hibernate:
        format_sql: true
management:
  endpoints:
    web:
      exposure:
        include: health, info